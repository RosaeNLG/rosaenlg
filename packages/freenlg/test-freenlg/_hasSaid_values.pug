
-
  util.expected = [
    '<t>',
      '<l>add1 add2 add3 6 / add1 add2 add3 12 / add1 add2 add3 18</l>',
    '</t>'
  ].join('');



mixin listOfStuff
  itemz {separator: ' '}
    item
      | add1
      - util.saidManager.has_said['TEST'] += 1;
    item
      | add2
      - util.saidManager.has_said['TEST'] += 2;
    item
      | add3
      - util.saidManager.has_said['TEST'] += 3;

mixin helper
  | #[+listOfStuff] #{util.saidManager.has_said['TEST']}

t
  - util.saidManager.has_said['TEST'] = 0;
  l #[+helper] / #[+helper] / #[+helper]
  - deleteSaid('TEST');

