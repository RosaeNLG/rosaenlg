-
  util.expected = [
    '<p>',
      //-'<l>A C C C C B A B B B C B B A B B C B C B</l>', <= sans poids
    '</p>'
  ].join('');


p
  seed #{util.randomSeed}
  l
    - var n = 0;
    //- 20      
    while n < 1
      synz
        syn
          | A
        syn {weight: 5}
          | B
        syn
          | C
      - n++;
