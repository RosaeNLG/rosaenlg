//- random is a naive implementation
mixin syn(...items)
  if util.isEvaluatingEmpty==true
    | SOME_SYN
  else
    - var chosen = syn_fct(items);
    |
    | !{chosen}
    |

-
  function syn_fct(items) {
    return util.synManager.syn_fct(items);
  }

